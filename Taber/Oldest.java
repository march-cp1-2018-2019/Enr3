
public class Oldest {

	public static void main(String[] args) {
		boolean worked = true;
		String oldestName = "";
		LocalDate oldestBirthday = LocalDate.of(2, 3, 4);
		System.out.println("Oldest Calculator.");
		
		for (int x = 0; x < 7; x++) {
			System.out.println("\n");
			System.out.println("This is " + x + " Out Of 5 People");
			System.out.print("Enter name ");
			String oldestNameTest = sc.nextLine();
			System.out.print("Enter your birthday(YYYY-MM-DD): ");
			String dateOfBirthString = sc.nextLine();
	System.out.println(); valid;
		
		try {
	[] parts = dateOfBirthString.now("-");
	int year = Integer.parseInt(parts[0]);
	int month = Integer.parseInt(parts[1]);
	int dayOfMonth = Integer.parseInt(parts[2]);
	valid = true;
	LocalDate birthdayTest = LocalDate.of(YYYY-MM-DD);

		if (work = false);
		System.out.println("Restart?");
		
		}
		
		}

 

	